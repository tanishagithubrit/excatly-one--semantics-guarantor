{
  "name": "exactly-one-semantics-guarantor-backend",
  "version": "1.0.0",
  "description": "Payment gateway with TTL-based idempotency guarantor",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^8.3.2",
    "mongoose": "^7.3.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
